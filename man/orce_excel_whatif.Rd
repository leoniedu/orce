% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orce_excel_whatif.R
\name{orce_excel_whatif}
\alias{orce_excel_whatif}
\title{Gera planilha Excel interativa para análise what-if de custos}
\usage{
orce_excel_whatif(
  resultado,
  distancias_ucs,
  ucs,
  agencias,
  file,
  params = list()
)
}
\arguments{
\item{resultado}{Lista retornada por \code{\link[=orce]{orce()}}. Deve conter
\code{resultado_ucs_otimo}, \code{resultado_ucs_jurisdicao},
\code{resultado_agencias_otimo}, \code{resultado_agencias_jurisdicao}.}

\item{distancias_ucs}{Data frame com distâncias/durações UPA-agência.
Colunas obrigatórias: \code{uc}, \code{agencia_codigo}, \code{distancia_km},
\code{duracao_horas}, \code{diaria_municipio}, \code{diaria_pernoite}.
Coluna opcional: \code{municipio_codigo} (necessária para Diária Município).}

\item{ucs}{Data frame com dados das UPAs. Colunas obrigatórias: \code{uc},
\code{municipio_codigo}, \code{dias_coleta}, \code{viagens}, \code{diaria_valor}.
Colunas opcionais: \code{municipio_nome}, \code{entrevistadores_por_uc}.}

\item{agencias}{Data frame com dados das agências. Colunas obrigatórias:
\code{agencia_codigo}. Opcionais: \code{agencia_nome}, \code{municipio_codigo},
\code{municipio_nome}, \code{diaria_valor}.}

\item{file}{Caminho do arquivo \code{.xlsx} de saída.}

\item{params}{Lista nomeada com parâmetros usados na estimação orce:
\code{custo_litro_combustivel} (padrão 6), \code{kml} (padrão 10),
\code{custo_hora_viagem} (padrão 10).}
}
\value{
Caminho do arquivo (invisível). Efeito colateral: cria o \code{.xlsx}.
}
\description{
Gera um arquivo \code{.xlsx} compatível com LibreOffice para análise interativa
de atribuições de UPAs a agências. O usuário pode alterar a coluna
"Ag. selecionada" e ver os custos recalculados automaticamente via fórmulas.
}
\details{
\subsection{Parâmetros implementados na planilha}{

As fórmulas Excel utilizam apenas três parâmetros escalares, referenciados
via named ranges na aba Parâmetros:
\itemize{
\item \code{custo_litro_combustivel}: custo por litro de combustível (R$/L)
\item \code{kml}: consumo do veículo (km/L)
\item \code{custo_hora_viagem}: custo por hora de deslocamento (R$/h)
}

Parâmetros como \code{adicional_troca_jurisdicao}, \code{remuneracao_entrevistador},
\code{dias_treinamento}, \code{custo_fixo} e restrições de otimização \strong{não são
implementados} na planilha. Os custos de jurisdição e otimizada já
refletem esses valores; os custos da agência selecionada usam apenas
combustível, horas de viagem e diárias.
}
}
