% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orce_restricoes.R
\name{orce_ucs_afetadas}
\alias{orce_ucs_afetadas}
\title{Identificar UCs Afetadas por Restrições}
\usage{
orce_ucs_afetadas(
  restricoes,
  resultado_ucs = NULL,
  ucs = NULL,
  alocar_por = "uc"
)
}
\arguments{
\item{restricoes}{Lista de restrições (mesmo formato de
\code{\link[=orce_aplicar_restricoes]{orce_aplicar_restricoes()}}).}

\item{resultado_ucs}{Data frame com o resultado atual da otimização,
contendo ao menos as colunas \code{uc} e \code{agencia_codigo}.}

\item{ucs}{(Opcional) Data frame de UCs contendo a coluna de agrupamento
especificada por \code{alocar_por}. Necessário quando \code{alocar_por != "uc"}.}

\item{alocar_por}{Coluna usada para agrupar UCs na otimização.
Padrão: \code{"uc"} (sem agrupamento).}
}
\value{
Vetor de caracteres com os códigos das UCs afetadas (sem duplicatas).
}
\description{
Retorna os códigos das UCs que são diretamente afetadas por uma lista de
restrições, considerando também UCs atribuídas a agências desativadas.
Quando \code{alocar_por} não é \code{"uc"}, expande as UCs afetadas para incluir
todas as UCs do mesmo grupo de alocação.
}
