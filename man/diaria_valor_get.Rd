% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diaria_valor_get.R
\name{diaria_valor_get}
\alias{diaria_valor_get}
\title{Obtém o Valor da Diária por Código do Município}
\usage{
diaria_valor_get(municipio_codigo, valores_diarias = c(425, 380, 335))
}
\arguments{
\item{municipio_codigo}{Vetor numérico ou caractere contendo código(s) de
município brasileiro (7 dígitos).}

\item{valores_diarias}{Vetor numérico de comprimento 3 especificando os valores
das diárias para cada nível. Padrão é c(425, 380, 335).}
}
\value{
Vetor numérico com os valores das diárias para os municípios especificados.
}
\description{
Recupera o valor da diária com base no código do município brasileiro. A função
categoriza os municípios em três níveis com valores correspondentes.
}
\details{
A função categoriza os municípios em três níveis:
\itemize{
\item Nível 1 (425): Capitais principais
\item Nível 2 (380): Outras capitais
\item Nível 3 (335): Demais municípios
}
}
\examples{
# Único município (Brasília)
valor_diaria_get("5300108")  # Retorna 425

# Múltiplos municípios
valor_diaria_get(c("5300108", "1302603", "1200013"))  # Retorna c(425, 380, 335)

# Usando valores personalizados
valor_diaria_get("5300108", c(500, 450, 400))  # Retorna 500

# Vetor de municípios com valores personalizados
valor_diaria_get(
  c("5300108", "1302603", "1200013"),
  c(500, 450, 400)
)  # Retorna c(500, 450, 400)

}
