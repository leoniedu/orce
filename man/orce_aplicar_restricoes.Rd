% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orce_restricoes.R
\name{orce_aplicar_restricoes}
\alias{orce_aplicar_restricoes}
\title{Aplicar Restrições Manuais aos Dados de Entrada do orce}
\usage{
orce_aplicar_restricoes(
  ucs,
  agencias,
  distancias_ucs,
  agencias_treinamento = NULL,
  restricoes = list()
)
}
\arguments{
\item{ucs}{Data frame de UCs (mesmo formato aceito por \code{\link[=orce]{orce()}}).}

\item{agencias}{Data frame de agências.}

\item{distancias_ucs}{Data frame de distâncias UC–agência.}

\item{agencias_treinamento}{Vetor de códigos das agências de treinamento
(ou \code{NULL}).}

\item{restricoes}{Lista de restrições. Cada elemento é uma lista com:
\describe{
\item{tipo}{Tipo da restrição: \code{"bloquear"}, \code{"forcar"},
\code{"desativar_agencia"}, ou \code{"agencias_treinamento"}.}
\item{uc}{Código(s) da(s) UC(s) afetada(s) (para \code{"bloquear"} e
\code{"forcar"}).}
\item{agencia_codigo}{Código da agência alvo (para \code{"bloquear"},
\code{"forcar"} e \code{"desativar_agencia"}).}
\item{agencias_treinamento}{Vetor de códigos das novas agências de
treinamento (para tipo \code{"agencias_treinamento"}).}
}}
}
\value{
Lista com os dados modificados:
\describe{
\item{ucs}{Data frame de UCs (inalterado).}
\item{agencias}{Data frame de agências (possivelmente filtrado).}
\item{distancias_ucs}{Data frame de distâncias (inalterado, exceto
remoção de agências desativadas).}
\item{agencias_treinamento}{Vetor atualizado de agências de treinamento.}
\item{fixar_atribuicoes}{Data frame com colunas \code{uc} e
\code{agencia_codigo} derivado de restrições \code{"forcar"}, ou \code{NULL}.}
\item{bloquear_atribuicoes}{Data frame com colunas \code{uc} e
\code{agencia_codigo} derivado de restrições \code{"bloquear"}, ou \code{NULL}.}
}
}
\description{
Modifica os data frames de entrada (\code{ucs}, \code{agencias}, \code{distancias_ucs}) e
o vetor \code{agencias_treinamento} de acordo com uma lista de restrições manuais,
permitindo refinar iterativamente um plano de alocação antes de re-otimizar
com \code{\link[=orce]{orce()}}.
}
\details{
As restrições são aplicadas na ordem fornecida. Cada tipo funciona assim:
\itemize{
\item \strong{\code{bloquear}}: Acumula pares (UC, agência) em \code{bloquear_atribuicoes},
que serão traduzidos em restrições rígidas \code{x[i,j] == 0} por \code{\link[=orce]{orce()}}.
\item \strong{\code{forcar}}: Acumula pares (UC, agência) em \code{fixar_atribuicoes},
que serão traduzidos em restrições rígidas \code{x[i,j] == 1} por \code{\link[=orce]{orce()}}.
\item \strong{\code{desativar_agencia}}: Remove a agência de \code{agencias} e de
\code{distancias_ucs}.
\item \strong{\code{agencias_treinamento}}: Substitui o vetor de agências de treinamento.
}
}
