% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_mip.R
\name{model_mip}
\alias{model_mip}
\title{Create a MIP Model for Warehouse TSP Optimization}
\usage{
model_mip(
  n,
  m,
  p,
  peso_tsp,
  n_entrevistadores_tipo,
  transport_cost_i_j,
  dist_uc_uc,
  duracao_uc_uc,
  kml,
  custo_litro_combustivel,
  custo_hora_viagem,
  dias_coleta_ijt,
  agencias_t,
  remuneracao_entrevistador,
  n_entrevistadores_min,
  dias_coleta_entrevistador_max,
  ti,
  diarias_entrevistador_max,
  diarias_i_j,
  ...
)
}
\arguments{
\item{n}{Integer. Number of collection units (UCs)}

\item{m}{Integer. Number of agencies}

\item{p}{Integer. Number of time periods}

\item{peso_tsp}{Numeric. TSP weight factor between 0 and 1. When 0, uses
simple round-trip costs. When > 0, enables TSP routing optimization}

\item{n_entrevistadores_tipo}{Character. Variable type for number of interviewers:
"continuous", "binary", or "integer"}

\item{transport_cost_i_j}{Matrix. Transportation cost from UC i to agency j (n x m)}

\item{dist_uc_uc}{Matrix. Distance matrix between UCs (n x n). Used when peso_tsp > 0}

\item{duracao_uc_uc}{Matrix. Duration matrix between UCs (n x n). Used when peso_tsp > 0}

\item{kml}{Numeric. Kilometers per liter fuel efficiency}

\item{custo_litro_combustivel}{Numeric. Cost per liter of fuel}

\item{custo_hora_viagem}{Numeric. Cost per hour of travel time}

\item{dias_coleta_ijt}{Function. Returns collection days for UC i at agency j in period t}

\item{agencias_t}{Data.frame. Agency data with columns: custo_fixo,
custo_treinamento_por_entrevistador, n_entrevistadores_agencia_max}

\item{remuneracao_entrevistador}{Numeric. Base salary per interviewer}

\item{n_entrevistadores_min}{Integer. Minimum number of interviewers per active agency}

\item{dias_coleta_entrevistador_max}{Integer. Maximum collection days per interviewer}

\item{ti}{Matrix. Time indicator matrix (n x p) indicating if UC i is active in period t}

\item{diarias_entrevistador_max}{Numeric. Maximum daily allowance per interviewer}

\item{diarias_i_j}{Matrix. Daily allowance matrix from UC i to agency j (n x m)}

\item{...}{Additional arguments (reserved for future use)}
}
\value{
An OMPR MIPModel object ready for solving
}
\description{
This function creates a Mixed Integer Programming (MIP) model for optimizing
warehouse collection unit assignments to agencies with optional TSP routing.
The model minimizes total costs including transportation, fixed costs, and
interviewer expenses.
}
\details{
The model includes:
\itemize{
\item Assignment variables: x\link{i,j} = 1 if UC i assigned to agency j
\item Agency activation: y\link{j} = 1 if agency j is active
\item Interviewer allocation: w\link{j} = number of interviewers at agency j
\item TSP routing (if peso_tsp > 0): route\link{i,k,j,t} and subtour elimination variables
}

Constraints ensure:
\itemize{
\item Each UC assigned to exactly one agency
\item Agency activation requirements
\item Minimum interviewer requirements
\item Capacity constraints
\item TSP constraints (if enabled)
\item Optional limits on interviewers and daily allowances
}
}
\examples{
\dontrun{
# Example with simple round-trip optimization (no TSP)
model <- model_mip(
  n = 10, m = 3, p = 2, peso_tsp = 0,
  n_entrevistadores_tipo = "integer",
  transport_cost_i_j = matrix(runif(30), 10, 3),
  # ... other parameters
)
}
}
